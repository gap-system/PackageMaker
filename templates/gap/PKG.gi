#
# {{PackageName}}: {{Subtitle}}
#
# Implementations
#
InstallGlobalFunction( {{PackageName}}_Example,
function()
	Print( "This is a placeholder function, replace it with your own code.\n" );
end );

