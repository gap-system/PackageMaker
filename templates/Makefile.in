#
# Makefile rules for the {{PackageName}} package
#
KEXT_NAME = {{PackageName}}
KEXT_SOURCES = src/{{PackageName}}.{{KERNEL_EXT_LANG_EXT}}

# include shared GAP package build system
GAPPATH = @GAPPATH@
include Makefile.gappkg
